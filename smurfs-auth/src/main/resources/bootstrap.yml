spring:
  cloud:
    config:
      discovery:
        serviceId: CONFIGSERVER
        enabled: true
      profile: prod
      name: AUTHSERVICE
      label: master
  cache:
    type: redis
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/